import { expect, assert } from 'chai';

import Game from './models/Game/game.model';
import { createRandomGame } from './utils';


describe('Bowling Game', () => {

  it('creating a random Game', () => {
    // const game: Game = createRandomGame();
  });

});
